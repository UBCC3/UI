export interface AvailableCalculation {
    id: number;
    name: string;
}

export interface AvailableBasisSet {
    id: number;
    name: string;
}

export interface AvailableMethod {
    id: number;
    name: string;
}
